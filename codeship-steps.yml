- name: unittests
  service: unittests
  command: "/data/unittests.sh"

- name: functionaltests
  service: functionaltests
  command: bash -c "sleep 10 && vendor/bin/behat --config=features/behat.yml --strict --stop-on-failure --append-snippets"