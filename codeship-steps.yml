- name: unittests
  service: unittests
  command: "/data/unittests.sh"

- name: functionaltests
  service: functionaltests
  command: bash -c "rm -f /data/composer.lock && whenavail idp 80 200 echo idp ready && /data/run-tests.sh"